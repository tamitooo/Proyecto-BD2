"""
Paquete de pruebas del MiniGestor de Base de Datos Multimodal (Parte 1).

Marcar `tests` como paquete permite que la raíz del repositorio quede en
`sys.path`, de modo que las pruebas importen los módulos del motor
(`storage`, `indexes`, `operators`, `query`, `benchmarks`) tanto con
pytest como con `python -m unittest discover -s tests -t .`.
"""
